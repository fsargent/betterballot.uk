{
  "name": "betterballot-uk",
  "version": "1.0.0",
  "type": "module",
  "description": "To Build a Better Ballot — UK: an interactive guide to fairer voting & proportional representation. A public-domain remix of Nicky Case's original.",
  "private": true,
  "license": "CC0-1.0",
  "scripts": {
    "start": "bun tools/static-server.ts",
    "build": "bun tools/build-play-js.ts",
    "check:generated": "bun tools/check-generated-output.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun run build && bun run check:generated && bun run typecheck && bun run test:unit && bun run test:browser",
    "test:unit": "bun test tests/*.test.ts",
    "test:browser": "playwright test",
    "dev": "wrangler dev",
    "deploy": "bun run build && wrangler deploy"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3",
    "wrangler": "^4"
  }
}
